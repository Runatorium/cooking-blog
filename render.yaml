services:
  # Django Backend Service
  - type: web
    name: sardegna-ricette-backend
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      cd frontend && npm install && npm run build && cd ..
      cd cooking_blog && python manage.py collectstatic --no-input
    # Start Gunicorn only - run migrations once via Shell: cd cooking_blog && python manage.py migrate
    startCommand: cd cooking_blog && gunicorn cooking_blog.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --access-logfile - --error-logfile -
    envVars:
      - key: DJANGO_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        fromDatabase:
          name: sardegna-ricette-db
          property: connectionString
      # Required: set to your frontend origin so registration/login work (e.g. https://sardegna-ricette-frontend.onrender.com)
      - key: FRONTEND_URL
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        sync: false
      - key: RENDER_EXTERNAL_HOSTNAME
        fromService:
          type: web
          name: sardegna-ricette-backend
          property: host

  # React Frontend Service (Static Site)
  - type: web
    name: sardegna-ricette-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: sardegna-ricette-backend
          property: host

databases:
  # PostgreSQL Database
  - name: sardegna-ricette-db
    databaseName: sardegna_ricette
    user: sardegna_ricette_user
